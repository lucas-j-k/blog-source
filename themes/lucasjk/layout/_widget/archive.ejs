<% if (site.posts.length){ %>
  <div class="widget__wrap">
    <h3 class="widget__title"><%= __('Archive') %></h3>
    <div class="widget__body">
      <%- list_archives({show_count: theme.show_count, type: theme.archive_type}) %>
    </div>
  </div>
<% } %>
